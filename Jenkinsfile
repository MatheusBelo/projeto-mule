pipeline {
    agent { dockerfile true }
    stages {
        stage('Teste de dockerfile') {
            steps {
                sh 'java --version'
                echo ' minha pipeline que lê dockerfile'
            }
        }
    }
}
